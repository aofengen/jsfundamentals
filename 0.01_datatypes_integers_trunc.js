//Math.trunc returns the integer portion of a number (the left side of the decimal). This is useful when inputs need to be read
//as a whole number.

let x = 7.32;
console.log(Math.trunc(x));

let y = .93737;
console.log(Math.trunc(y));